// Centralized in-memory store for fallback mode (when MongoDB is not available)
export const mockUsers = [];
export const mockHistory = {}; // { userId: [Array] }
export const mockFavorites = {}; // { userId: [Array] }
